# Mamba-TransQR inference and deployment configuration.
model:
  image_size: 256
  patch_size: 16
  embed_dim: 256
  depth: 8
  num_heads: 8
  mamba_backend: mamba_ssm
  mamba_d_state: 16
  mamba_d_conv: 4
  mamba_expand: 2

inference:
  checkpoint: checkpoints/best.pt
  device: auto
  mixed_precision: true
  decoder_backend: null
  output_dir: outputs
  save_original: true

export:
  format: torchscript
  output_path: exports/mambatransqr.pt
  dynamic_batch: true
  opset_version: 17
